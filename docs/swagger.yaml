basePath: /api
definitions:
  models.GetTrackRequest:
    type: object
host: localhost:8080
info:
  contact: {}
  description: none
  title: task API
  version: "1.0"
paths:
  /task/track:
    post:
      consumes:
      - application/json
      description: 获取指定用户的轨迹信息
      parameters:
      - description: 用户轨迹请求
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/models.GetTrackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.GetTrackRequest'
      summary: 获取用户轨迹
swagger: "2.0"
